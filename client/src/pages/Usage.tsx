import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { Coins, ListTodo } from "lucide-react";
export default function Usage() { const balance = trpc.credits.balance.useQuery(); const jobs = trpc.jobs.list.useQuery(); return <DashboardLayout><div className="space-y-8 p-5 lg:p-8"><div><p className="text-sm text-cyan-600">USAGE & CREDITS</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Operational ledger</h1><p className="mt-2 text-sm text-muted-foreground">P/AI Credits are separate from provider token units and long-running work is tracked as jobs.</p></div><div className="grid gap-4 md:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Coins className="size-5 text-cyan-700" /> Available credits</CardTitle></CardHeader><CardContent><div className="text-4xl font-semibold">{balance.data?.balance ?? 0}</div><p className="mt-2 text-xs text-muted-foreground">Ledger-backed balance</p></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><ListTodo className="size-5 text-cyan-700" /> Jobs</CardTitle></CardHeader><CardContent><div className="text-4xl font-semibold">{jobs.data?.length ?? 0}</div><p className="mt-2 text-xs text-muted-foreground">Your tracked operations</p></CardContent></Card></div><Card><CardHeader><CardTitle>Recent jobs</CardTitle></CardHeader><CardContent className="space-y-3">{jobs.data?.length ? jobs.data.map(job => <div key={job.id} className="flex items-center justify-between rounded-lg border p-4"><div><div className="font-medium">{job.operationType}</div><div className="text-xs text-muted-foreground">Job #{job.id} · {job.progress}%</div></div><Badge variant="secondary">{job.status}</Badge></div>) : <p className="text-sm text-muted-foreground">No jobs yet.</p>}</CardContent></Card></div></DashboardLayout>; }
