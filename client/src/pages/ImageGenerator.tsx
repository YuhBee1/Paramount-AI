import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { ImagePlus, Sparkles } from "lucide-react";
import { useState } from "react";
export default function ImageGenerator() { const [prompt, setPrompt] = useState(""); const [url, setUrl] = useState<string | undefined>(); const generate = trpc.media.image.useMutation({ onSuccess: result => setUrl(result.url) }); return <DashboardLayout><div className="space-y-8 p-5 lg:p-8"><div><p className="text-sm text-cyan-600">MULTIMODAL STUDIO</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Image generation</h1><p className="mt-2 text-sm text-muted-foreground">Generate an asset through the configured server-side provider. Quality is fixed to the standard launch setting.</p></div><div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Sparkles className="size-5 text-cyan-700" /> Prompt</CardTitle></CardHeader><CardContent className="space-y-4"><Textarea value={prompt} onChange={event => setPrompt(event.target.value)} placeholder="Describe the image you want…" className="min-h-40" /><Button className="w-full bg-slate-950 text-white" disabled={!prompt.trim() || generate.isPending} onClick={() => generate.mutate({ prompt })}><ImagePlus className="mr-2 size-4" />{generate.isPending ? "Generating…" : "Generate image"}</Button>{generate.error && <p className="text-sm text-destructive">{generate.error.message}</p>}</CardContent></Card><Card className="overflow-hidden"><CardHeader><CardTitle>Generated asset</CardTitle></CardHeader><CardContent className="flex min-h-80 items-center justify-center bg-muted/30">{url ? <img src={url} alt={prompt} className="max-h-[520px] max-w-full rounded-lg object-contain" /> : <div className="text-center text-sm text-muted-foreground"><ImagePlus className="mx-auto mb-3 size-8 opacity-50" />Your generated image will appear here.</div>}</CardContent></Card></div></div></DashboardLayout>; }
